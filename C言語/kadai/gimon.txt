/* next順に表示。末尾wp->nextが先頭アドレスstartを指すときloop終了 */
for( honnmono = start ; honnmono->next != start ; honnmono = honnmono->next ){

  for( nisemono = honnmono->next ; nisemono->next != start ; nisemono = nisemono->next ){

    if( strcmp(honnmono->service_name, nisemono->service_name) < 0 ) {


      //strcpy(honnmono->service_name, nisemono->service_name) ;
      //strcpy(honnmono->port_number, nisemono->port_number) ;
      //strcpy(honnmono->protocol, nisemono->protocol) ;




    }
    else if( strcmp(honnmono->service_name, nisemono->service_name) == 0 ) {

      break ;
    }
    else if( strcmp(honnmono->service_name, nisemono->service_name) < 0 ) {

      break ;

    }
  }
}
